# Локально: cp .env.example .env
# ONREZA: nrz env set KEY=value --env production

TELEGRAM_BOT_TOKEN=
# Один или несколько chat_id через запятую
TELEGRAM_CHAT_ID=
# Прокси к Telegram API (Cloudflare Worker), если прямой api.telegram.org недоступен
TELEGRAM_API_BASE=https://studio1903-telegram-proxy.<account>.workers.dev
